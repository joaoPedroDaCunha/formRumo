@model Rumo.Models.Verser

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Verser</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Vehicle)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.VehicleId)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.id"><button class="btn btn-secondary">Edit</button></a> |
    <a asp-controller="Aet" asp-action="Details" asp-route-id="@Model?.AetId"><button class="btn btn-secondary">Back</button></a>
</div>
